# Razorpay Configuration (optional)
# If not configured, the app will use mock payment flow
VITE_RAZORPAY_KEY=your_razorpay_key_here

# Caricature API Configuration (optional)
# When VITE_CARICATURE_API_URL is NOT set or empty:
#   → The app defaults to same-origin /caricature route (canister-hosted)
# When VITE_CARICATURE_API_URL is set (e.g., http://localhost:3001/caricature):
#   → The app posts to that custom URL instead
#
# The upload endpoint must accept multipart/form-data with TWO fields:
#   1. 'image' - the photo file (not 'file' or any other name)
#   2. 'style' - the selected art style string (e.g., "3D Pixar", "Renaissance Oil", etc.)
#
# Example for local Node server:
# VITE_CARICATURE_API_URL=http://localhost:3001/caricature
#
# Leave blank to use canister route:
VITE_CARICATURE_API_URL=

# Optional API key for authentication
VITE_CARICATURE_API_KEY=your_api_key_here

